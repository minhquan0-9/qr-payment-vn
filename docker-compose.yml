services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: payment
      POSTGRES_PASSWORD: payment
      POSTGRES_DB: payments
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U payment -d payments"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://payment:payment@postgres:5432/payments
    ports:
      - "8000:8000"
    command: >
      sh -c "alembic upgrade head &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000 --proxy-headers"
    restart: unless-stopped

  # ===== Worker per-bank =====
  # Bật bằng profile, ví dụ:
  #   docker compose --profile mb up
  #   docker compose --profile mb --profile acb up
  #   docker compose --profile mb --profile acb --profile tpb up

  worker-mb:
    build: .
    profiles: ["mb", "all"]
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://payment:payment@postgres:5432/payments
      BANK_TYPE: mb
    command: ["python", "-m", "app.worker"]
    restart: unless-stopped

  worker-acb:
    build: .
    profiles: ["acb", "all"]
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://payment:payment@postgres:5432/payments
      BANK_TYPE: acb
    command: ["python", "-m", "app.worker"]
    restart: unless-stopped

  worker-tpb:
    build: .
    profiles: ["tpb", "all"]
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://payment:payment@postgres:5432/payments
      BANK_TYPE: tpb
    command: ["python", "-m", "app.worker"]
    restart: unless-stopped

volumes:
  pgdata:
